# 课程 Agent 助教 · 架构设计（概要）

- 版本：v0.1｜日期：2026-09-14｜状态：草稿（**待修订**）
- 对应图：[architecture-client-server.svg](./diagrams/architecture-client-server.svg)（端云交互总览）、[architecture-local-agent.svg](./diagrams/architecture-local-agent.svg)（本地 Agent 详细设计）
- 上游文档：[../01-requirements.md](../01-requirements.md)（需求 v1.0.1）
- 相关：[adr/](./adr/README.md)（架构决策记录）。原第 5 节的 D1–D5 已正式化为 ADR-0001 ~ 0005

> ⚠️ **本文档 v0.1 有两处已被挑战，修订之前不要据此开工**：
>
> 1. **第 3.3 节与 D2 写的"subagent 是独立 pi 进程"**——经源码验证，官方 subagent 示例的进程隔离会导致权限闸无法被继承（子进程 `--mode json` 下 `hasUI = false`），结果是"要么子代理全被拒，要么闸被绕过"。见 [adr/0006](./adr/0006-subagent-process-model.md)（**提议中**）
> 2. **第 3.4 节的权限闸实现机制需要细化**——挂点方向正确，但"主/subagent 共用同一道闸"以及"判定不被 handler 篡改"需要按 [adr/0007](./adr/0007-permission-gate-hook-point.md)（**提议中**）与 [permission-gate.md](./permission-gate.md) 写实

## 1. 总体形态

一句话：**Electron 桌面端（学生电脑）+ 一个轻服务端**。agent 内核以 SDK 方式内嵌 pi（跑在 Electron 主进程）；服务端只做知识库、检索、记录三件事，不感知 agent 内部结构。模型调用由学生自己的 API key 直连供应商，内容不经手服务端。

## 2. 端云边界与通道（对应端云图）

三条 HTTP 通道，除此之外两端互不越界：

| # | 通道 | 方向 | 载荷 | 时机 |
|---|---|---|---|---|
| ① | 检索 | 本地 → 服务端 → 本地 | 问题 → 相关片段 + 出处（文件名 + 页码） | agent 答题需要资料时，同步 |
| ② | 激活 | 本地 → 服务端 | 邀请码 + 姓名学号 → 课程 token | 首次使用，一次性 |
| ③ | 记录上报 | 本地 → 服务端 | 提问 / 反馈 / 拦截事件 | 异步攒批，可关，学生可查 |

数据归属：

- **仅存本地**：会话 JSONL、项目代码、课程 token、权限配置、审计日志
- **仅存服务端**：课程知识库、标准答案与题库（不落学生盘）、使用记录
- **直连模型供应商**：学生提问内容 + 检索到的片段（学生自己的 key，服务端不可见）

边界原则：权限闸是本地端所有"出圈动作"的唯一通道；GUI 只经事件流读、经指令 API 写；工具层内只有检索工具知道服务端存在；服务端不感知多智能体结构。

## 3. 本地端结构（对应本地详图）

Electron 应用 = 渲染进程（GUI）+ 主进程（Node，承载以下全部模块），两进程之间走 IPC（事件下行推送 / 指令上行）。

### 3.1 GUI（渲染进程）四个视图

- **对话视图**：消息流、流式渲染、subagent 过程树、出处卡片（资料名 + 页码）、一键反馈
- **会话中心**：会话列表（空闲 / 运行中 / 待确认）、历史会话入口（pi -r 的界面化）、新建 / 切换
- **权限中心**：三档设置、确认弹窗（含 subagent 动作）、动作日志
- **激活与设置**：激活向导（邀请码 + 姓名学号）、告知同意（首次）、API key 配置、工作区信息

### 3.2 Agent 宿主（主进程，SDK 内嵌 pi）

- **实例池**：每会话一实例；懒创建（发出首条消息 / 打开历史会话时才建）；**切换会话不销毁**（后台任务继续跑，支持多会话并行布置任务）；运行中永不回收；空闲约 10 分钟 LRU 回收（时长可配置）。实例只是缓存，磁盘账本才是唯一事实——重建 = SessionManager 读 JSONL 重组上下文，秒级、无模型调用
- **会话装配器**：调 pi SessionManager 读 JSONL → 组装上下文 → 构造实例；同时是"历史会话入口"的数据源
- **事件总线**：把实例事件流推送给 GUI；steer / followUp / abort 接口是 GUI 指令的落点

### 3.3 subagent（独立 pi 进程，pi 官方扩展机制）

- **通用 subagent 基座**（markdown 定义）：能力完整，承接主 agent 拆分的长时复杂任务，干完只回传摘要
- **资料检索 subagent**：基座 + 工具白名单（只留检索）+ 只读；作为上下文防火墙，检索大结果在它自己的进程里消化，防止撑爆主 agent
- 模式：**专用 = 基座 + 工具限制 + 提示词聚焦**；"只读"锁死在工具配置层，不靠提示词

### 3.4 权限闸（自研扩展，挂在 beforeToolCall 钩子，主 / subagent 共用）

流水线：**拦截点 → 白名单**（项目目录内常规读写、常见安全命令 → 放行）**→ 黑名单**（大面积删除、出项目目录、联网、全局安装 → 弹确认）**→ 灰区判官**（小 agent 评危险系数；拿不准 = 危险；学生的问题、代码输出属不可信内容，不得影响判断）**→ 三档执行**（变更前确认[默认] / 自动审批 / YOLO）**→ 审计日志**（全部动作落盘可查）。

档位决定"要不要先问"，不决定"要不要告诉你"。

### 3.5 工具集

- **pi 自带**：read / edit / bash——看代码、复现报错、改文件
- **自研**：课程资料检索——通道形态已定：**自研扩展工具（pi.registerTool）+ HTTP**；服务端接口按 MCP 友好格式预留。超时降级为提示

### 3.6 服务端客户端

激活（邀请码 → token）、检索调用、记录上报（本地队列攒批、可关、学生可查看）。

### 3.7 本地数据

会话 JSONL（按课程工作区目录隔离）、审计日志、配置（权限档位 / API key / 课程 token）。

## 4. 服务端结构（简）

四个服务 + 一个存储：**接入**（邀请码、学生登记、token 签发）、**知识库**（上传、解析/切分/索引，保留出处元数据）、**检索**（问题 → 片段 + 出处；标准答案与题库不在此通道返回；向量 + FTS5 混合检索）、**记录**（事件存档、期末导出）；存储用 SQLite 单库（sqlite-vec 向量索引 + FTS5 关键词索引）。部署在自己租用的云主机。

## 5. 决策记录

原 D1–D5 已正式化为 ADR（每条补齐了"备选方案"与"后果"两节），本文档不再重复正文：

| 原编号 | ADR | 主题 |
|---|---|---|
| D1 | [ADR-0001](./adr/0001-electron-desktop.md) | 本地端采用 Electron 桌面应用 |
| D2 | [ADR-0002](./adr/0002-embed-pi-as-sdk.md) | 以 SDK 方式内嵌 pi（**其中"subagent 独立进程"一句已被 ADR-0006 挑战**） |
| D3 | [ADR-0003](./adr/0003-server-on-rented-vm.md) | 服务端部署在自租云主机 |
| D4 | [ADR-0004](./adr/0004-rag-hybrid-retrieval.md) | RAG 采用混合检索 |
| D5 | [ADR-0005](./adr/0005-retrieval-tool-extension.md) | 检索通道用自研扩展工具 + HTTP |
| — | [ADR-0006](./adr/0006-subagent-process-model.md) | 子代理的进程模型（**提议中**） |
| — | [ADR-0007](./adr/0007-permission-gate-hook-point.md) | 权限闸的挂点与审批链路（**提议中**） |

**待拍板：ADR-0006、ADR-0007。** 这两条不拍板，第 3.3 / 3.4 节写不实。
下一步：详细设计（服务端接口契约、权限闸实现方案）——已建骨架，见 [permission-gate.md](./permission-gate.md)、[server-api.md](./server-api.md)、[../README.md](../README.md)。

## 6. 需求映射（v1 ↔ 模块）

| 需求 | 落在哪个模块 |
|---|---|
| 检索课程资料 | 3.5 自研检索工具 + 3.6 客户端 + 服务端知识库/检索 |
| 读写项目文件、执行命令 | 3.5 pi 自带工具（经 3.4 权限闸） |
| 多智能体 | 3.3 subagent（基座 + 派生） |
| 回答有出处 | 检索片段自带出处 + "引用只从片段来"约束 |
| 干活过程实时可见 | 3.2 事件总线 → 3.1 对话视图 |
| 硬规矩 2/3/4/6 | 第 2 节数据归属 + 3.4 审计日志 + 3.6 可关可查 |
