# GUI 详细设计

> 版本：v0.1（骨架）｜ 状态：骨架 ｜ 日期：2026-09-14
> 上游：[../01-requirements.md](../01-requirements.md) 第 2 节、[architecture.md](./architecture.md) 第 3.1 节、[permission-gate.md](./permission-gate.md)
> 下游：[ipc-contract.md](./ipc-contract.md)

## 1. 范围

渲染进程（Electron renderer）的全部界面。主进程的东西不在这里——事件与指令的字段定义见 [ipc-contract.md](./ipc-contract.md)。

## 2. 技术栈【待决】

| 项 | 候选 | 说明 |
|---|---|---|
| 框架 | React / Vue / 原生 | 【待决】 |
| 状态管理 | | 【待决】 |
| 构建 | Vite / esbuild | 【待决】 |
| 样式 | | 【待决】 |
| 流式 Markdown 渲染 | | 需要处理增量输出 + 代码高亮 |

**决策影响**：选型会直接决定 [ipc-contract.md](./ipc-contract.md) 里"事件怎么送到 UI"的写法（状态库订阅 vs 事件总线）。

## 3. 四个视图

### 3.1 对话视图

- 消息流 + 流式渲染
- **子代理过程树**（实时可见，可展开）
- **出处卡片**（资料名 + 页码；点击可看片段原文）
- 一键反馈（回答不对时）
- 权限确认弹窗（见 3.3）

【待填】消息的数据结构、流式渲染的增量协议、过程树的层级表示

### 3.2 会话中心

- 会话列表（空闲 / 运行中 / 待确认）
- 历史会话入口（`pi -r` 的界面化）
- 新建 / 切换（**切换不销毁实例**）

【待填】列表项的字段、状态如何反映主进程真实状态

### 3.3 权限中心

- 三档设置（默认：变更前确认）
- **确认弹窗** —— 要能显示：
  - 谁在请求（`actor`：主 agent / 哪个子代理）
  - 请求什么（工具名 + 参数；`bash` 要显示完整命令）
  - 为什么问（命中黑名单？灰区判官判为危险？）
  - 选项：允许 / 拒绝 /（可能）本次会话内总是允许
  - **倒计时**（超时 = 拒绝，见 permission-gate.md 第 10 节）
- 动作日志（全部可追溯）

【待填】弹窗的三种来源形态（主 agent / 子代理 / 灰区判官）、多个请求排队时的展示

### 3.4 激活与设置

- 激活向导（邀请码 + 姓名学号）——**【待决】只服务自己时是否砍掉**
- 告知同意（首次）——**【待决】同上**
- API key 配置（学生自选供应商）
- 课程工作区目录展示

## 4. 关键交互约定

| 场景 | 行为 |
|---|---|
| agent 正在干活 | 实时显示在读哪个文件、跑什么命令 |
| 弹窗超时 | 按拒绝处理 |
| 学生中止 | 能中止主 agent 或**指定某个子代理** |
| 子代理动作 | 与主 agent 一样可见、可中止 |
| 引用出处 | 只显示来自检索片段的出处；资料没有时显示"资料里没有，以下是模型的理解" |

## 5. 与权限闸的接口

GUI 不直接调用闸。所有审批经 `ApprovalService` → IPC（见 [ipc-contract.md](./ipc-contract.md)）。

**要求**：GUI 崩溃或卡住时，闸的行为必须是**拒绝**而不是挂起或放行。

## 6. 待决问题

| # | 问题 |
|---|---|
| Q1 | 技术栈全套 |
| Q2 | 子代理过程树的呈现形式（缩进树 / 时间线 / 卡片） |
| Q3 | 出处卡片的交互（点击展开片段原文？跳转到资料？） |
| Q4 | 三档设置放哪（权限中心 / 设置里 / 两处都有） |
| Q5 | 是否需要"会话列表"这个视图（只服务自己时可能不需要） |

## 7. 参考

- [architecture.md](./architecture.md) 第 3.1 节（四视图的来源）
- `packages/coding-agent/src/modes/interactive/` —— pi 官方 TUI 的组件实现，可参考信息组织方式
- `packages/coding-agent/src/modes/rpc/rpc-mode.ts:136-208` —— 对话框转协议的参考
